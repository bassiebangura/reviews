 <%=form_with(model: [@movie, @review], local:true) do |f|%>
        <%= f.label :name %>
        <%= f.text_field :name, autofocus: true %>
        <%= f.label :stars %>
        <%= f.select :stars, Review::STARS, prompt: "Pick One" %>
        <%= f.label :comment, rows: 20 %>
        <%= f.text_area :description %>
        <%= f.submit "Post Review" %>
<% end %>
