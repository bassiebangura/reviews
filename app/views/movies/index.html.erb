

<% @movies.each do |movie| %>
   <section class="movie">
   <div class="summary">
       <h2><%= movie.title %></h2></div> 
      <h3><%= format_total_gross(movie) %></h3>
      </i>
      <br/>
      <p><%= truncate(movie.description, length: 50, separator: ' ') %></p>
 </div>
      </section>
<% end %>


